import{useEffect,useState}from"react";import{supabase}from"@/integrations/supabase/client";export function PublicCreatorStories({creatorUserId}:{creatorUserId:string}){const[x,setX]=useState<any[]>([]);useEffect(()=>{void(async()=>{const{data}=await(supabase.rpc as any)("public_creator_stories",{p_creator_user_id:creatorUserId});setX(data||[])})()},[creatorUserId]);if(!x.length)return null;return <section className="mx-auto max-w-7xl px-6 pb-14"><p className="text-sm font-semibold uppercase tracking-widest text-primary">Stories</p><div className="mt-5 grid gap-4 md:grid-cols-2">{x.map(s=><article key={s.id} className="rounded-2xl border bg-card p-6"><h2 className="text-xl font-semibold">{s.title}</h2><p className="mt-3 whitespace-pre-line text-sm leading-6 text-muted-foreground">{s.body}</p></article>)}</div></section>}

