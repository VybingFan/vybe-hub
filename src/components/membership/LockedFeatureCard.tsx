import type{ReactNode}from"react";import{Link}from"@tanstack/react-router";import{LockKeyhole}from"lucide-react";import{Button}from"@/components/ui/button";import{Card,CardContent}from"@/components/ui/card";import{CREATOR_PLAN_CATALOG,type PublicCreatorPlanCode}from"@/features/membership/catalog";
export function LockedFeatureCard({title,description,requiredPlan,usage,children,compact=false}:{title:string;description:string;requiredPlan:PublicCreatorPlanCode;usage?:string;children?:ReactNode;compact?:boolean}){const plan=CREATOR_PLAN_CATALOG.find(x=>x.code===requiredPlan);return <Card className="border-primary/25 bg-gradient-to-br from-primary/5 via-background to-violet-500/5"><CardContent className={compact?"p-4":"p-5"}><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="flex items-center gap-2 font-semibold"><LockKeyhole className="h-4 w-4 text-primary"/>{title}</p><p className="mt-1 text-sm text-muted-foreground">{description}</p>{usage&&<p className="mt-2 text-xs font-medium text-primary">{usage}</p>}{children}</div><Button asChild size={compact?"sm":"default"}><Link to="/creator-memberships">View {plan?.name??"membership"}</Link></Button></div></CardContent></Card>}

