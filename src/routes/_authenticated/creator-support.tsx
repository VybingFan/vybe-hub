import{FormEvent}from"react";import{createFileRoute}from"@tanstack/react-router";import{toast}from"sonner";import{supabase}from"@/integrations/supabase/client";import{Card,CardContent}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{Textarea}from"@/components/ui/textarea";import{Button}from"@/components/ui/button";export const Route=createFileRoute("/_authenticated/creator-support")({component:Page});function Page(){const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget),{data:{user}}=await supabase.auth.getUser();if(!user)return;const{error}=await(supabase.from("creator_support_requests")as any).insert({creator_user_id:user.id,category:"creator_plus",subject:f.get("subject"),details:f.get("details"),priority:"priority"});error?toast.error(error.message):toast.success("Priority support request received")};return <Card className="mx-auto max-w-2xl"><CardContent className="space-y-4 p-6"><h1 className="text-3xl font-semibold">Creator Plus Support</h1><form onSubmit={submit} className="space-y-4"><Input name="subject" required placeholder="What do you need help with?"/><Textarea name="details" required/><Button>Send priority request</Button></form></CardContent></Card>}
