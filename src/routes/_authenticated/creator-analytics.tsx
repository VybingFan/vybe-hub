import{useEffect,useState}from"react";import{createFileRoute}from"@tanstack/react-router";import{supabase}from"@/integrations/supabase/client";import{Card,CardContent}from"@/components/ui/card";export const Route=createFileRoute("/_authenticated/creator-analytics")({component:Page});function Page(){const[d,setD]=useState<any>({});useEffect(()=>{void(async()=>{const{data}=await(supabase.rpc as any)("get_my_creator_activity",{p_days:90});setD(data||{})})()},[]);return <div className="mx-auto max-w-4xl"><h1 className="text-4xl font-semibold">90-day creator activity</h1><div className="mt-6 grid gap-4 sm:grid-cols-4">{[["Followers",d.followers],["Likes",d.likes],["Saves",d.saves],["Comments",d.comments]].map(([k,v])=><Card key={k as string}><CardContent className="p-5"><p className="text-3xl font-semibold">{v||0}</p><p className="text-sm text-muted-foreground">{k}</p></CardContent></Card>)}</div></div>}

