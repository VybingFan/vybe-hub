# VYBE V24.47D2R4 — Appearance Preview Fix

- Leaves the existing VYBE Light/Dark system untouched.
- Keeps only Midnight Blue and Warm Stage as added Appearance choices.
- Fixes the browser preview by applying the extra palette directly to root CSS variables.
- Selecting “Use standard VYBE colors” removes only the extra overrides.
- No database migration.
- No staging or commit.
